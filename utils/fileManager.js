const fs = require('fs');


/*exports.deleteImage = (filename) => {

    fs.unlink('./images/' + filename, (err) => {
        console.log('test start');
        if (err) return false;
        else {
            return true;
        }

    });

}*/